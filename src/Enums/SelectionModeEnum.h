#ifndef SELECTION_MODE_H
#define SELECTION_MODE_H

namespace enums {

enum class SelectionModeEnum {
    PORTFOLIO,
    CREATE_WALLET,
    LOAD_SD,
    LOAD_SEED,
    INFOS,
    COUNT
};

}

#endif // SELECTION_MODE_H

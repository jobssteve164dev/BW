# BW 安全模型与审计记录

最近审计日期：2026-09-21  
审计基线：`f6ee10f93eaecbb5d894a914280be742d4a50b92`

BW 是离线签名器，不是具备安全芯片认证的硬件钱包。安全目标是：无线接口保持关闭；秘密默认不持久化；确需持久化时采用带认证的加密；只为经过设备屏幕核对、且输入与当前钱包匹配的交易签名。

## 受保护资产和边界

最高敏感资产包括助记词熵、BIP39 附加密码、保险库密码、派生私钥，以及用户在屏幕上确认的交易意图。

不可信输入包括 SD 卡上的钱包文件、助记词文件、保险库和 PSBT，RFID 标签内容，USB 主机，以及 GitHub 构建链下载的第三方依赖。屏幕和 Cardputer 键盘是本项目用于确认交易的可信交互边界。

## 已执行的控制

| 编号 | 风险 | 已落地控制 | 验收条件 |
| --- | --- | --- | --- |
| SEC-01 | 恶意大文件或超大目录耗尽 RAM，造成重启或签名流程异常 | 所有 SD 文本、PSBT 和保险库读取均在分配前检查大小，并在短读、读取错误或实际内容越界时拒绝且清空结果；单目录最多展示 128 项，最多缓存 4 个目录 | 原生边界测试覆盖正常、超限、短读、读取失败和声明大小不一致 |
| SEC-02 | 保险库被离线猜解 | 新保险库使用 16 字节随机盐、PBKDF2-HMAC-SHA256 600,000 次和 AES-256-GCM；新密码至少 12 字符；旧保险库仍可按文件内工作因子读取 | 认证标签或头部被修改后解锁失败；旧格式不因工作因子升级失效 |
| SEC-03 | RFID 内容被读取、篡改、半写入或通过低成本口令猜解 | 新 RFID 备份强制至少 12 字符密码，使用 PBKDF2-HMAC-SHA256 600,000 次和 AES-256-GCM；写入前先使旧标记失效，所有密文、盐和标签校验成功后才提交新标记；不再创建明文标签 | 新格式区分 16/32 字节种子；任一中间载荷阶段失败均不提交有效标记，提交回读不确定时仅可能留下完整且可认证的记录；错误密码、标签或密文篡改均不能产生种子 |
| SEC-04 | 随机盐含 `0x00` 时被截断，导致 RFID 备份无法恢复 | RFID 盐值全程使用固定 16 字节二进制数组，不再按 C 字符串截断 | 含零字节的盐仍生成完整 12 字节 nonce 并可参与派生 |
| SEC-05 | 弱随机数、未检查随机函数失败或未定义移位影响种子生成 | 真随机采样期间显式启用 ESP32-S3 内部熵源；检查 CTR-DRBG 返回值；备份抽查索引使用硬件随机；修正 32 位移位未定义行为并真正混入麦克风 GPIO 状态 | 随机 API 失败会中止，不会继续生成钱包；无线保持关闭时仍使用官方支持的内部熵源 |
| SEC-06 | 临时密钥材料残留在普通缓冲区 | 派生密钥、SHA-256 栈缓冲、助记词熵缓冲和生成中间量在使用后覆写；移除一次无用途的助记词熵副本；减少助记词和附加密码的按值复制 | 本项目明确持有的临时明文缓冲在相关成功和失败路径返回前覆写；第三方库内部副本仍归入物理内存剩余风险 |
| SEC-07 | 构建依赖漂移或工作流供应链被替换 | Arduino 框架固定到完整提交，PlatformIO 库固定到精确版本，GitHub Actions 固定到完整提交；普通构建只有只读仓库权限，发布写权限隔离到标签发布任务 | 配置测试拒绝移动版本范围、可移动 Action 标签和全局写权限 |
| SEC-08 | 未核对交易或被替换签名结果 | 仅接受主网 BIP84 原生 SegWit、当前钱包拥有且 UTXO 证据一致的输入；屏幕逐项显示全部输出和手续费；仅接受 `SIGHASH_ALL`；写盘前比对未签名交易并只合并新增签名 | 金额溢出、UTXO 冲突、非本钱包路径、交易被替换或签名未新增时全部拒绝 |
| SEC-09 | 无线侧信道扩大攻击面 | 启动时停止并卸载 Wi-Fi，停用并释放 BLE 控制器；任一状态不能确认为隔离即停止启动 | 无线命令失败、仍为活动状态或状态不确定时均 fail closed |

## 兼容与迁移

- 旧保险库仍可解锁；下一次成功写入时会以 600,000 次工作因子重新加密。
- 旧 RFID 明文标签仍可读取，但设备会明确警告。旧 AES-ECB 标签仅为迁移保留，读取后会提示立即重新创建备份。
- 新固件不会再创建明文 RFID 备份，也不会再创建旧 AES-ECB 格式。
- 固件不会向 SD 写入明文助记词；为兼容已有备份仍可显式导入 `.txt` 助记词，但读取前会警告并要求确认。
- `/card-wallets.txt`、NVS 钱包路径、收款地址、zpub、指纹和派生路径均为公开信息，不应被当成秘密。

## 无法由通用固件自动消除的剩余风险

| 风险 | 等级 | 原因与当前处置 |
| --- | --- | --- |
| 设备被长期物理控制后读取 Flash、调试口或运行内存 | 高 | 通用可刷固件不能替每台设备安全地烧录独立 Secure Boot 密钥、Flash Encryption 密钥或不可逆 eFuse。自动启用会导致设备锁死和所有用户共用密钥。应保持设备物理受控，使用后关机；高价值资金不应在未完成独立密钥注入的设备上使用。 |
| 功耗、电磁、键盘观察、屏幕摄影等物理侧信道 | 高 | Cardputer ADV 没有安全元件、遮蔽键盘或经实验室验证的抗侧信道硬件。软件关闭无线不能消除物理测量。操作时应隔离摄像设备和不可信 USB 主机。 |
| MIFARE Classic 标签可复制且使用默认认证密钥 | 中 | 新格式依靠 AES-GCM 和高成本口令保护内容，不把卡片访问控制当安全边界；标签丢失仍允许离线猜密码，因此必须使用长且唯一的密码。 |
| 老旧嵌入式比特币解析依赖中的未知缺陷 | 中 | 子模块被固定到精确提交，并由输入上限、双解析、钱包所有权检查和签名后合并校验包围；但依赖年代较早，尚无覆盖整个解析面的持续模糊测试。 |
| 构建产物只有 SHA-256、没有项目独立签名密钥 | 中 | 校验和可发现传输损坏，但不能在 GitHub 账户或发布链被攻破时提供独立真实性证明。只从仓库正式 Actions 下载，并核对运行对应提交。 |
| 恶意 SD 卡可回滚到旧的、但认证有效的保险库备份 | 低 | GCM 能发现篡改但不能证明文件的新旧；当前设备没有安全单调计数器。钱包 zpub 会在解锁后重新派生核对，回滚主要造成旧记录缺失或可用性问题。 |

这些剩余风险不能包装成“软件已解决”。在 Secure Boot、Flash Encryption、JTAG/下载模式策略、独立发布签名和真机侧信道测试完成前，BW 仍应按实验性离线签名器使用，并先用测试钱包和小额资金验证。

## 安全使用要求

1. 第一次创建钱包时离线操作，只在纸上备份助记词和 BIP39 附加密码。
2. 保险库和 RFID 使用不同的、至少 12 字符的唯一密码；不要把密码保存在同一张 SD 卡上。
3. 签名前在 Cardputer 屏幕逐项核对全部收款地址、金额和手续费；任何一项不符都取消。
4. 只连接可信 USB 电源或可信主机；不需要 USB 输入时不要连接电脑。
5. 使用后退出签名流程并关机，不把已解锁设备交给他人。
6. 优先从本仓库正式 Release 获取固件；使用 Actions 时只选择已核对提交号的 `main` 分支运行，不使用 Pull Request 产物，并核对 `SHA256SUMS.txt`。

发现漏洞请不要公开助记词、保险库或真实交易材料；通过仓库的私密漏洞报告渠道提交最小复现。
